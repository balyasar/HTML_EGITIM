server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/javahs1_eticaret
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
